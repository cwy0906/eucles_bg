<!DOCTYPE html>
<html>
  <head>
    <title>EuclesBg</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "sidebars", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-dark">
    <div class="container-fluid">

      <div class="row h-100">

        <div class="col-md-2 col-xs-1 bg-light">
          <%= render partial: "common_ parts/main_sidebars" %>
        </div>

        <div class="col-md-10 col-xs-11 py-2">
          <div class="row">
            <div class="col-md-12 col-xs-12" style="height: 140px;">
              <%= render partial: "common_ parts/message_zone", locals: { common_message: (session[:common_message].deep_symbolize_keys rescue nil) } %>
            </div>
            <div class="col-md-12 col-xs-12 py-2">
                <%= yield %>
            </div>
          </div>
        </div>

      </div>

    </div>
  </body>
</html>
